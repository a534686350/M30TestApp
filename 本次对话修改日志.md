# 本次对话修改日志

记录时间：2026-05-25 ~ 2026-05-26

## 1. 手动调试页：串口列表改为读取电脑实际串口

修改文件：

- `src/M30TestApp.Wpf/ViewModels/ManualViewModel.cs`
- `src/M30TestApp.Wpf/Views/ManualView.xaml`
- `src/M30TestApp.Wpf/M30TestApp.Wpf.csproj`

修改内容：

- 将原先固定列出的 COM 口改为调用 `System.IO.Ports.SerialPort.GetPortNames()` 获取电脑实际存在的串口。
- 新增 `ComPorts` 集合。
- 新增 `RefreshComPortsCommand`。
- 在采集卡串口和烘箱串口旁增加“刷新”按钮。
- 新增 `System.IO.Ports` NuGet 包引用。

## 2. 手动调试页：修复数据采集指令生成逻辑

修改文件：

- `src/M30TestApp.Wpf/ViewModels/ManualViewModel.cs`

修改内容：

- 按原 WinForms 程序逻辑恢复采集卡读数指令。
- 采集指令格式按 `采集卡地址 + 功能码 + 通道地址 + Modbus CRC` 生成。
- 功能码对应关系：
  - `2`：读取 `USource`
  - `3`：读取 `ISource`
  - `4`：读取 `USignal`
  - `5`：读取 `UT`
- 新增 Modbus CRC 计算方法：
  - `BuildCrcFrame`
  - `AppendCrc`
  - `ModbusCrc`
  - `ToHex`
- TX/RX 显示格式调整为接近原程序：
  - `Send: ...`
  - `Get: CRC校验后：...`

注意：

- 曾短暂加入过随机模拟采集值用于测试重复读取显示变化。
- 用户说明已连接真实设备后，已撤回随机模拟值改动。

## 3. 手动调试页：加入工位选择并自动带出板卡/通道地址

修改文件：

- `src/M30TestApp.Wpf/ViewModels/ManualViewModel.cs`
- `src/M30TestApp.Wpf/Views/ManualView.xaml`

修改内容：

- 在“读取数据 / 通道选择”区域加入“工位”下拉框。
- 选择工位后自动根据 `工位对应表.csv` 中的映射填写：
  - 采集卡地址：`BoardSlotNo`
  - 通道地址：`FixtureSlotNo`
- 地址范围按原程序恢复为 `1` 到 `16`。

## 4. 手动调试页：数据 I/O 与历史记录自动滚动

修改文件：

- `src/M30TestApp.Wpf/Views/ManualView.xaml.cs`

修改内容：

- 对 `DataIo` 和 `History` 集合变更进行监听。
- 新增自动滚动到最新记录逻辑。
- 后续根据反馈，将滚动动作改为通过 `Dispatcher.BeginInvoke(DispatcherPriority.Loaded, ...)` 延迟执行，避免 UI 虚拟化和布局未完成导致无法滚到底部。

## 5. 配置页：补回原程序设置界面

修改文件：

- `src/M30TestApp.Wpf/ViewModels/ConfigViewModel.cs`
- `src/M30TestApp.Wpf/Views/ConfigView.xaml`

修改内容：

- 在配置页新增“参数控制”Tab。
- 新增原程序对应设置模块：
  - 接口设置 `Interface Settings`
  - 气路设置 / 阀门配置
  - 温度采集 / 切换单元设置
  - 温度采集设置
  - 时间参数设置
  - 压力控制器型号 / 指令编辑
- 新增配置 ViewModel 数据：
  - 串口列表
  - 波特率、数据位、校验位、停止位选项
  - GPIB 地址和端口选项
  - 切换单元卡号 `301-316`
  - 阀门通道 `101-109`
  - 温度采集通道 `201-204`
  - 压力控制器指令模板

## 6. 构建验证情况

已执行：

```powershell
dotnet build e:\test\M30TestApp.V2\M30TestApp.V2.sln -v:minimal
```

结果：

- 配置页修改完成后曾构建成功：`0 个警告，0 个错误`。
- 后续修复自动滚动后再次构建时失败，原因不是代码错误，而是程序正在运行，生成文件被进程锁定：
  - 锁定进程：`M30TestApp.V2`
  - 锁定文件：`src/M30TestApp.Wpf/bin/Debug/net8.0-windows/M30TestApp.V2.exe`

处理方式：

- 关闭正在运行的 `M30TestApp.V2` 后重新执行构建即可。

## 7. 当前需要注意的问题

- 已新增真实硬件设备层，后续不应再优先使用模拟设备。
- GPIB 设备通过 Keysight/IVI VISA COM 组件迟绑定访问，需要运行电脑安装 Keysight IO Libraries Suite。

## 8. 全部设备切换为真实设备

修改文件：

- `src/M30TestApp.Core/Devices/Hw/HwDevices.cs`
- `src/M30TestApp.Core/Devices/DeviceFactory.cs`
- `src/M30TestApp.Core/M30TestApp.Core.csproj`
- `samples/setting/Setting.ini`
- `src/M30TestApp.Wpf/bin/Debug/net8.0-windows/setting/Setting.ini`
- `src/M30TestApp.Wpf/ViewModels/ManualViewModel.cs`

修改内容：

- 新增真实硬件设备实现：
  - `HwOven`
  - `HwDac`
  - `HwPressureController`
  - `HwDmm`
  - `HwPowerSupply`
  - `HwBoard`
- 新增 VISA COM 迟绑定通讯封装：
  - `VisaSession`
- 新增串口辅助解析：
  - `SerialHelpers`
- `DeviceFactory` 已按 `Setting.ini` 中的 `HW` / `SIM` 创建对应设备。
- `M30TestApp.Core` 目标框架改为 `net8.0-windows`。
- `M30TestApp.Core` 增加 `System.IO.Ports` 引用。
- `Setting.ini` 默认切换为真实设备：
  - `Pressure = HW`
  - `Oven = HW`
  - `Dmm = HW`
  - `Dac = HW`
  - `Power = HW`
  - `Board = HW`
- 根据当前设备连接情况修正地址：
  - 压控：`GPIB1::10::INSTR`
  - 烘箱：`COM5`, `19200`, `8N1`
  - DMM/温度采集：`GPIB0::9::INSTR`
  - 采集卡：`COM3`, `9600`, `8N1`
  - 电源：`GPIB0::10::INSTR`
  - 板卡：`COM3`, `9600`
- 手动调试页的 DAC 读取已改为调用 `_session.Dac` 真实设备接口，不再使用固定模拟值。
- 烘箱设温已按原程序逻辑修复：
  - 发送 `TEMP,S25.0`
  - 收到 `OK` 后发送 `POWER,ON`
  - 停止命令对应 `POWER,OFF`

验证：

```powershell
dotnet build e:\test\M30TestApp.V2\M30TestApp.V2.sln -v:minimal
```

结果：

- `0 个警告`
- `0 个错误`

## 9. 阀门控制修复

问题：

- 手动页阀门按钮只更新 UI 和日志，没有发送真实硬件命令。
- 当前 Keysight Connection Expert 中在线的 34970A 是 `GPIB1::9::INSTR`，不是 `GPIB0::9::INSTR`。

修复：

- 阀门 1~8 改为调用真实 34970A 继电器控制。
- 通道映射按源程序 `GasProfile.xml`：
  - 阀1：`103`
  - 阀2：`102`
  - 阀3：`104`
  - 阀4：`105`
  - 阀5：`106`
  - 阀6：`107`
  - 阀7：`108`
  - 阀8：`109`
- 打开阀门发送 `ROUT:CLOSE (@通道)`。
- 关闭阀门发送 `ROUT:OPEN (@通道)`。
- 阀门操作失败时会弹窗并自动恢复 UI 开关状态。
- DMM/34970A 地址修正为 `GPIB1::9::INSTR`。
- 烘箱命令读取从 `ReadLine()` 改为 `ReadExisting()`，避免设备无换行时出现 timeout。

---

## 以下为 2026-05-26 修改内容

## 10. 停止按钮不响应修复

修改文件：

- `src/M30TestApp.Wpf/ViewModels/TestRunViewModel.cs`

修改内容：

- 1秒定时器中增加 `CommandManager.InvalidateRequerySuggested()` 调用。
- 使 `CancelCommand` 的 `CanExecute` 在长时间运行的单条命令中也能被刷新。
- 停止按钮在测试运行期间现在可以正常点击。

## 11. 温度/压力实时显示修复

修改文件：

- `src/M30TestApp.Wpf/ViewModels/TestRunViewModel.cs`

修改内容：

- 1秒定时器中增加从 `session.Context` 读取 `CurrentTemp` 和 `CurrentPressure` 并刷新到 UI。
- 解决了 `Run:PerformanceTest` 作为单条命令执行时中间状态不显示的问题。

## 12. PlanTitle 更新修复

修改文件：

- `src/M30TestApp.Wpf/ViewModels/MainViewModel.cs`

修改内容：

- 订阅 `session.Reconfigured` 事件。
- 方案重新配置后通过 Dispatcher 通知 UI 更新 `PlanTitle` 属性。

## 13. Run:PerformanceTest 完整实现

修改文件：

- `src/M30TestApp.Core/TaskScript/Actions/BuiltinActions.cs`

修改内容：

- 重写 `RunPerformanceTestAction.ExecuteAsync`，实现完整测试流程：
  - 探漏：逐阀加压检测泄漏率，全开整体探漏
  - 逐温度点：设置烘箱 → 等待到达（最长240min，1分钟日志）→ 保温（1分钟日志显示剩余）→ 采集UT
  - 逐压力点：设置压力 → 稳定 → 保压 → 逐工位开阀采集 USC/ISC/USG
  - 完成后保存数据、泄压
- 新增辅助方法：
  - `PerformLeakCheckAsync` — 逐阀探漏
  - `SetAndWaitPressureAsync` — 设压并等待稳定
  - `ReadPointWithValveAsync` — 逐工位开阀采集
  - `ReadSingleMeasure` — 单项测量
  - `SetAndWaitOvenAsync` — 加热等待（1分钟间隔日志，240min超时）
  - `SoakWithLogAsync` — 保温日志（每分钟显示剩余时间）

## 14. TaskScript 预览展开

修改文件：

- `src/M30TestApp.Wpf/ViewModels/RunSetupViewModel.cs`

修改内容：

- `PlanTaskPreview` 属性增加对 `Run:PerformanceTest` 的特殊处理。
- 当脚本为单条 `Run:PerformanceTest` 时，根据方案的温度点/压力点动态展开为详细的步骤描述。
- 新增 `ExpandPerformancePreview` 方法，生成包含探漏、逐温度逐压力的完整流程预览。

## 15. 新增"跳过探漏"选项

修改文件：

- `src/M30TestApp.Wpf/ViewModels/RunSetupViewModel.cs`
- `src/M30TestApp.Wpf/Views/RunSetupWindow.xaml`
- `src/M30TestApp.Core/TaskScript/TaskContext.cs`
- `src/M30TestApp.Wpf/ViewModels/TestRunViewModel.cs`

修改内容：

- 新增 `UseLeakCheck` 属性和"启用探漏"复选框，位于设备启用区域。
- `TaskContext` 增加 `SkipLeakCheck` 标志。
- 运行时根据复选框状态设置 `SkipLeakCheck`，探漏步骤据此决定是否跳过。
- 运行结束后在 finally 中恢复 `SkipLeakCheck` 原值。

## 16. 探漏只测实际用到的阀门

修改文件：

- `src/M30TestApp.Core/TaskScript/Actions/BuiltinActions.cs`
- `src/M30TestApp.Wpf/ViewModels/RunSetupViewModel.cs`

修改内容：

- `PerformLeakCheckAsync` 根据 `ctx.Slots.Entries` 中的 `Valve` 字段筛选实际用到的阀门编号。
- 只对用到的阀门进行逐阀探漏，不再遍历全部8个阀门。
- 只有1个阀门时跳过"全开整体探漏"步骤（冗余）。
- `Regenerate()` 中修正阀门分配逻辑：每32个工位递增一次阀门编号（1阀门=32工位，2阀门/层，4层=256工位）。

## 17. 烘箱温度读取 NaN 修复

修改文件：

- `src/M30TestApp.Core/Devices/Hw/HwDevices.cs`

修改内容：

- `HwOven.ReadTempAsync` 原先直接对整个串口响应做 `float.TryParse`。
- 烘箱实际返回逗号分隔多值如 `24.8,25.0,155.0,-75.0`，导致解析失败返回 NaN。
- 修复为先用逗号分割取第一个值（当前温度），再做 `float.TryParse`。

## 18. 加热等待 NaN 容错

修改文件：

- `src/M30TestApp.Core/TaskScript/Actions/BuiltinActions.cs`

修改内容：

- `SetAndWaitOvenAsync` 增加 NaN 计数器。
- 连续3次读温返回 NaN 或异常时，打印警告并跳过加热等待直接进入保温。
- 避免烘箱通讯异常时死等240分钟。

## 19. 设备初始化（运行前连接测试）

修改文件：

- `src/M30TestApp.Core/TaskScript/Actions/BuiltinActions.cs`

修改内容：

- `Run:PerformanceTest` 开始时新增 `InitDevicesAsync` 步骤。
- 依次尝试 `OpenAsync` 所有设备：压控、烘箱、DMM、采集板、电源、板卡。
- 连接成功后触发 `StateChanged` 事件，顶部状态栏自动从"离线"变为"在线"。
- 连接失败不中断测试，仅记录警告日志。
- Board 和 Dac 共享串口时跳过重复打开。

## 20. 工位配置持久化

修改文件：

- `src/M30TestApp.Wpf/ViewModels/RunSetupViewModel.cs`

修改内容：

- `Apply` 时将所有布局参数（SlotCount、BatchNo、StartIndex、StartBoard、StartBoardSlot、BoardSlotCapacity、StartValve、FixtureSlotCapacity、FixtureCount、StartChannel、StartSerial、LastPlan）保存到 `Setting.ini` 的 `[Slots]` 节。
- `SeedFromCurrentSlots` 优先从 `Setting.ini` 恢复上次保存的布局参数。
- 通过 `LastPlan` 标记判断是否有用户保存的历史配置；有则使用 ini 值，无则使用 session 默认值。
- 解决了每次打开对话框工位数被重置的问题。

## 21. 主题改版：Cockpit Tools 风格

修改文件：

- `src/M30TestApp.Wpf/Themes/Light.xaml`
- `src/M30TestApp.Wpf/MainWindow.xaml`
- `src/M30TestApp.Wpf/Views/TestRunView.xaml`

修改内容：

- 整体色调改为浅蓝渐变背景（左上 `#D6EAF8` → 右下 `#F0F7FC`），仿 Cockpit Tools 风格。
- 顶部栏改为白色渐变毛玻璃效果，带柔和阴影。
- 侧边栏改为浮动卡片样式，右侧圆角 16px，带阴影。
- 导航按钮改为胶囊形（10px 圆角），悬浮时变蓝色背景 + 蓝色文字。
- 卡片统一改为无边框、12px 圆角、`DropShadowEffect` 阴影。
- 按钮圆角从 4px 增大到 8px，主按钮悬浮时阴影加深。
- Ghost 按钮悬浮时边框变蓝。
- 文本框增加圆角 6px 模板，悬浮/聚焦时边框变蓝。
- TabItem 选中时带半透明蓝色背景，圆角顶部。
- DataGrid 网格线改为浅色 `#EBF0F5`，去除外边框。
- 设备状态胶囊改为浅灰底 + 1px 细边框，更精致。
- KPI 卡片改为 12px 圆角 + 阴影，无边框。
- 新增资源：`BgGradientBrush`、`TopBarBrush`、`CardShadow`、`CardShadowHover`、`NavShadow`、`AccentLightBrush`、`NavHoverBrush`。
- 新增 ProgressBar 圆角样式、CheckBox 手型光标、ScrollBar 窄化样式。

## 22. 设置页面（仿 Cockpit Tools）

新增文件：

- `src/M30TestApp.Wpf/Views/SettingsView.xaml` — 设置页面 UI
- `src/M30TestApp.Wpf/Views/SettingsView.xaml.cs` — 代码后台 + 值转换器
- `src/M30TestApp.Wpf/ViewModels/SettingsViewModel.cs` — 设置 ViewModel
- `src/M30TestApp.Wpf/Strings/zh-CN.xaml` — 中文字符串资源
- `src/M30TestApp.Wpf/Strings/en-US.xaml` — 英文字符串资源

修改文件：

- `src/M30TestApp.Wpf/ViewModels/MainViewModel.cs` — 新增 SettingsViewModel 和导航命令
- `src/M30TestApp.Wpf/MainWindow.xaml` — 侧边栏添加"设置"导航按钮（带分隔线）
- `src/M30TestApp.Wpf/App.xaml` — 注册 SettingsViewModel DataTemplate 和默认中文资源

功能内容：

- **通用 Tab**：
  - 显示语言：简体中文 / English 下拉选择，切换后实时更新界面文字
  - 应用主题：当前仅浅色，预留深色入口
  - 语言偏好保存到 `Setting.ini` 的 `[App] Language` 键
- **关于 Tab**：
  - 仿 Cockpit Tools 居中布局：渐变 Logo、应用名、版本号
  - 检查更新按钮：通过 GitHub Releases API 获取最新版本号，比对当前版本
  - 发现新版本时弹窗询问是否打开下载页
  - 四宫格卡片：作者、开源仓库（点击跳转 GitHub）、意见反馈（跳转 Issues）、项目地址

## 23. Git 初始化 + 推送到 GitHub

- 安装 Git for Windows 2.54.0
- 创建 `.gitignore`（排除 bin/obj/.vs/log 等）
- 初始化仓库并提交所有源码
- 推送到 `https://github.com/a534686350/M30TestApp.git`
